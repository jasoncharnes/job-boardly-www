<div class="bg-white pt-20">
  <div class="mx-auto max-w-7xl px-6 lg:px-8">
    <div class="mx-auto max-w-2xl">
      <h1 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl text-center"><%= content(:heading) %></h1>
      <p class="mt-3 text-lg leading-8 text-gray-600 text-center"><%= content(:subheading) %></p>
      <div class="mt-10 space-y-16 border-t border-gray-200 pt-10 sm:mt-16 sm:pt-16">
        <%= render partial: "default/pages/post", collection: posts %>
      </div>
    </div>
  </div>
</div>

<%= render "default/shared/cta" %>
